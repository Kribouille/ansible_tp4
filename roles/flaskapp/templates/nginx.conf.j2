server {
    listen 80;

    server_name {{ app.domain }};

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/{{ app.user }}/{{ app.name }}/{{ app.name }}.sock;
    }
}
